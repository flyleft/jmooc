package me.jcala.jmooc.ctrl;

import com.alibaba.druid.support.http.StatViewServlet;

import javax.servlet.annotation.WebInitParam;
import javax.servlet.annotation.WebServlet;

/*
@WebServlet(urlPatterns = "/druid*/
/*",
        initParams={
                //@WebInitParam(name="allow",value="127.0.0.1,192.168.10.2"),// IP白名单 (没有配置或者为空，则允许所有访问)127.0.0.1,192.168.10.2
                @WebInitParam(name="deny",value="192.168.10.9"),// IP黑名单 (存在共同时，deny优先于allow)
                @WebInitParam(name="loginUsername",value="zuyi"),// 用户名
                @WebInitParam(name="loginPassword",value="zuyikali"),// 密码
//                @WebInitParam(name="resetEnable",value="false")// 禁用HTML页面上的“Reset All”功能
        })
public class DruidMonitorServlet extends StatViewServlet {

}
*/
